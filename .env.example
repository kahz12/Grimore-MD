# Grimoire — local-first runtime configuration
# Only OLLAMA_HOST is consumed today. Remote backends are unimplemented;
# do NOT add API keys here until cognition.allow_remote is supported.
OLLAMA_HOST=http://localhost:11434
